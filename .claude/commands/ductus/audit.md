---
description: Audit framework artifacts for cross-doc, cross-manifest, cross-registry drift. Maintainer-only.
---

<!-- audit:ignore-placeholders:file -->
<!-- This command is maintainer-only and not scaffolded into adopter projects,
     so its /ductus: references are literal, not templating drift. -->

# Audit

Audit `ductus`'s own framework artifacts for the kinds of drift `/ductus:analyze` is not scoped to catch. Maintainer-only — adopters never invoke this command. Runs without a session target.

## Purpose

`/ductus:analyze` audits a single feature spec's artifacts against each other (frontmatter, plan, tasks, data-model, dependencies, rule citations). Its contract is bounded to one feature directory plus declared dependencies, so it cannot see drift across the framework: pipeline diagrams in the constitution vs. the introduction, `configure/claude.md` vs. `configure/auggie.md` canonical permission set, migration registry vs. procedure files, etc.

`/audit` fills that gap. It loads no rule files — its checks are about *framework consistency*, not spec quality. Each check family produces structured findings on stdout. Exit code is binary: `0` when no findings, `1` when any finding is present. CI uses the exit code as a release gate.

See [spec 026](https://github.com/stonean/ductus/blob/main/specs/026-framework-self-audit/spec.md) for the design and the [026 plan](https://github.com/stonean/ductus/blob/main/specs/026-framework-self-audit/plan.md) for the check families and the check-zero precondition pass. The family set has grown well past the original design — `scripts/audit/run-all.sh` runs exactly the families enumerated in the markdown-only reference below, a correspondence Family 28 holds in both directions. No count is stated here on purpose: a number in prose is a third copy of a fact two lists already carry, and it is the copy nothing updates (this sentence said twenty-two while thirty-three ran). Family numbers are stable identifiers: Family 3 (registry equivalence) was retired with the workflows feature (spec 043), leaving a numbering gap.

## Scope Boundaries

- Read-only against the framework's cross-cutting artifacts. Do NOT modify any file.
- No session target required; the command operates on the framework as a whole.
- Reference: §drift-prevention, §principles. The constitution is loaded by other pipeline commands; `/audit` re-reads it independently because it runs without `/ductus:target`.

## Instructions

> **For agent runtimes**: the Invoke steps below call the MCP tools of the ductus runtime; the host-integration contract — bare↔prefixed tool names, lazy ToolSearch schema fetch, the no-shell-utilities rule, and the two-paths guarantee — lives once in the constitution, §runtime-host-integration. Before the server is registered — the window between acquisition and the restart that loads it — walk the same prose using the host file-reading tools (Read, Edit, Write).

1. Invoke `run-generator` against `scripts/audit/run-all.sh` — the orchestrator that runs the check-zero precondition pass followed by the family check scripts. The script emits findings to stdout under per-family headers and exits 0 (no findings) or 1 (any family produced findings).

Otherwise, walk the markdown-only path below.

## Markdown-only reference

When the runtime is not on `PATH`, walk the same scripts directly. Each prints findings to stdout and exits `0` (no findings) or `1` (findings present). Aggregate across all families; `/audit`'s exit code is the logical OR.

1. Run `scripts/audit/check-zero.sh` — generator/lint precondition. Halt on findings; do not run family checks against known-stale generator output.
2. Run `scripts/audit/cross-doc-consistency.sh` (Family 1).
3. Run `scripts/audit/manifest-parity.sh` (Family 2).
4. Run `scripts/audit/placeholder-roundtrip.sh` (Family 4).
5. Run `scripts/audit/template-alignment.sh` (Family 5).
6. Run `scripts/audit/ssot-invariants.sh` (Family 6).
7. Run `scripts/audit/sibling-coupling.sh` (Family 7).
8. Run `scripts/audit/introducing-drift.sh` (Family 8).
9. Run `scripts/audit/primitive-promotion-candidates.sh` (Family 9).
10. Run `scripts/audit/migration-coverage.sh` (Family 10).
11. Run `scripts/audit/consolidation-pair.sh` (Family 11).
12. Run `scripts/audit/fixture-session-shape.sh` (Family 12).
13. Run `scripts/audit/runtime-hardcoded-paths.sh` (Family 13).
14. Run `scripts/audit/installer-registry-parity.sh` (Family 14 — `install.sh` agent list and dest paths match the `ductus.md` **Agent Registry**, and each agent's pre-seeded settings file matches its registry `settings_template`).
15. Run `scripts/audit/runtime-probe-parity.sh` (Family 15 — the ductus binary probe is in parity between each agent's **Agent Registry** `settings_template` seed and its `configure/{key}.md` set: present in both or neither, never one only).
16. Run `scripts/audit/installer-command-parity.sh` (Family 16 — the `/ductus` **Per-Agent Scaffolding** slash-command manifest lists exactly the `framework/commands/*.md` files, minus the maintainer-only commands (`audit`) intentionally not shipped to adopters).

17. Run `scripts/audit/host-namespace-parity.sh` (Family 17 — the namespace the runtime renders (`[host] project`, else the repo directory basename, as `Host::load` resolves it) matches a namespace actually installed under an agent config dir, so no rendered next-action names a namespace the operator cannot invoke).

18. Run `scripts/audit/marker-list-parity.sh` (Family 18 — the `criterion-path-existence` non-assertion marker list agrees across its canonical source, the runtime array, and the adopter-facing restatement in `analyze.md`, including the spelled-out counts; a derivation that yields no markers is a finding rather than a silent pass).

19. Run `scripts/audit/review-freshness.sh` (Family 19 — no `done` spec ships with a review that predates its own code: a spec whose durable contracts changed since its `review.reviewed-against` sha is stale, which every other check passes because they assert only that a review exists and does not block).

20. Run `scripts/audit/version-agreement.sh` (Family 20 — the repo-root `version` pin, `runtime/Cargo.toml`, and the newest `runtime/CHANGELOG.md` heading carry the same SemVer. The release tag is deliberately not compared: the release commit precedes the tag push, so asserting it here would fail every release mid-flight).

21. Run `scripts/audit/transitional-bootstrap-parity.sh` (Family 21 — the retired `framework/bootstrap/govern.md` path stays byte-identical to `framework/bootstrap/ductus.md`. Every pre-rename adopter's self-update fetch resolves to the retired path, so drift there ships stale content to them verbatim and a deletion 404s their run before migrations).

22. Run `scripts/audit/adopter-shell-behavior.sh` (Family 22 — the shipped adopter shell works in an adopter's tree, not just in ours. Stands up a fixture with a non-default `[paths] specs-root`, config only at the converged tier, and the runtime reachable only through `.ductus/bin/ductus`, then runs the real `framework/bootstrap/hooks/ductus-pre-commit` in it. This repo runs *different copies* of that job — its own `.githooks/pre-commit`, the default spec root, a locally built runtime — so every assumption those mask is invisible to a green run here; three silent defects reached adopters through that gap on 2026-08-17. The runtime is stubbed, keeping the family hermetic and identical in CI).

23. Run `scripts/audit/sweep-target-manifest-parity.sh` (Family 23 — the live-artifact enumeration a rename sweep greps, delimited in `AGENTS.md` by `<!-- audit:sweep-targets:begin -->` / `<!-- audit:sweep-targets:end -->`, covers every source path the **Shared Files** manifest ships. A list naming a relocated directory sends the grep somewhere clean, so the sweep misses the files that moved and exits 0 — which is how 042's move of the generators to `.ductus/scripts/` survived 049's sweep. Runs manifest → list only: it proves no shipped file goes unswept, not that the list is complete, and says so. An extraction that yields nothing is a finding rather than a pass).

24. Run `scripts/audit/rename-sweep-residue.sh` (Family 24 — a word-boundary rename that replaced an ordinary English verb with the project's proper noun. 049's `govern` → `ductus` sweep could not tell the two apart, leaving sentences like "a class of behavior the framework should ductus at the rules tier". Eight sites survived; three ship to adopters, and one is a `create`-strategy template written into a new adopter's `AGENTS.md` once and corrected by no later run. Detection is exact rather than heuristic, drawn from two closed word classes: a modal followed by the project name (a modal takes a bare infinitive; a proper noun is not one), and the project name followed by a demonstrative or wh-word (a proper noun does not take one). All 23 families then existing were green throughout — the residue was found by an ad-hoc grep during an unrelated review, which is the gap this family closes).

25. Run `scripts/audit/unbalanced-inline-markup.sh` (Family 25 — an inline span that opens and never closes, which markdownlint cannot see: an unclosed backtick never becomes a code span, so `MD038` does not apply, and a literal `**` followed by a space is not a heading, list, or link. Two of the 21 `AGENTS.md` entries 050 rewrote broke this way and survived a clean review until a reader noticed the rendered text. Scoped per-line to the two rule files whose bullets are single and unwrapped (69 bullets, 0 continuation lines), where a span that opens on a line must close on it; deliberately **not** widened to prose that wraps, which would trade two real findings for the 283 correct cross-line spans measured there. The scope is reported on stderr, because a clean exit means "these two files are balanced" and must never read as "all markdown is balanced". A continuation line in those files is itself a finding — the per-line check is exact only while the convention holds, and narrowing quietly once the subject drifts is the failure this directory exists to prevent).

26. Run `scripts/audit/broken-relative-links.sh` (Family 26 — a relative markdown link whose target does not exist, falling between `MD051` (which validates heading *fragments* and never checks that the file exists) and `check-orphaned-references` (which scopes to adopter-owned referrers and ductus-managed prefixes), so a spec linking a sibling at the wrong depth is outside both. The dominant class is a depth error in a scenario file, which lives one tier deeper than its spec: one `../` too few renders fine, reviews fine, and resolves to nothing. **The check is the `check-corpus-links` primitive and this script is the entry point** — the Family 30 shape. It carried its own implementation until that primitive shipped for adopters, and the two diverged within the day: the primitive resolved a root-absolute target against the repository root while the family still resolved it against the filesystem root, discarding the citing file's directory entirely. Delegating makes that divergence impossible rather than merely fixed. The family passes `--scope repository`, so its subject is every *tracked* `.md` file — framework sources, scripts, this suite's own documentation — while the primitive's default subject is the spec corpus an adopter's pre-commit hook checks; one resolver, two subjects, and the difference is an argument rather than a second copy of the rule. The examined count is reported on stderr because it is the guard: consolidating could have silently narrowed the subject to the spec corpus, which is `QUAL-CLAIM-001` in its purest form, so 457 files before must stay 457 after. Generated command copies, project templates, runtime test fixtures, and the spec root's templates are excluded by construction and **counted**, never silently dropped. An unreachable runtime and an unparseable result are each findings, never a silent pass).

27. Run `scripts/audit/done-spec-criteria.sh` (Family 27 — a spec at `status: done` whose Acceptance Criteria still carry an unchecked box. The completion gate is supposed to make this unreachable: `implement.md` marks each verified criterion and refuses to propose the transition while any remains unchecked, so reaching `done` with one unticked means the gate was bypassed or its marking step failed. 026 did exactly that in `e9262df` and every signal stayed green — `run-all.sh` exited 0, `check-artifacts` reported the feature clean, CI passed — until a hand-written grep found it. Only `done` specs are examined, since unchecked criteria are the expected state everywhere else; status is read from the frontmatter block and the checkbox from the Acceptance Criteria section, never by a loose grep for either token, and fenced blocks are skipped so a document quoting checkbox syntax is not reported as carrying one. The finding names both repairs — tick it, or reopen the spec — because the family cannot tell which is right. Specs are enumerated from git with untracked ones skipped and counted, and the examined `done`-spec count is reported, so a clean exit says what it examined; an empty enumeration is a finding rather than a pass).

28. Run `scripts/audit/audit-family-parity.sh` (Family 28 — the family set `run-all.sh` registers, the set this list enumerates, and the scripts `scripts/audit/README.md` lists must agree. They drifted: `run-all.sh` registered 25 families while this list stopped at 23, so Families 24, 25, and 26 were absent from it in any form — the three added most recently, which is the direction this drift always runs, since wiring a family into `run-all.sh` is what makes it execute and the doc update is the step with no consequence if skipped. Registered-but-undocumented and documented-but-unregistered are reported separately because the repairs differ, and the second is the worse one: a family dropped from `run-all.sh` whose entry survives here reads as still running. Both sets are derived rather than hardcoded — a hardcoded expectation would be a third copy of the fact under test — and compared as `(number, script)` pairs, so an entry naming the right family against the wrong script is caught too. Retired numbers need no allowance: Family 3 is spent and appears in neither list, so the sets agree. Only these numbered entries count, so prose mentions of a family number do not, and the generated copy of this file is not a subject. An empty derivation on either side is a finding, because two empty sets compare equal).

29. Run `scripts/audit/permission-wildcard-position.sh` (Family 29 — a canonical **allow** entry that places its wildcard before the subcommand. `Bash(git -C * status *)` reads as "git status in another worktree" and is not: the leading `*` spans inserted options, so `git -C . -c core.pager='!sh -c "…"' status` matches it and is approved with no prompt, and both `-c` and `--exec-path` run arbitrary commands. Seven entries of that shape shipped in `configure/claude.md` and reached every adopter that ran `/ductus:configure`; the host's own startup linter is what surfaced them, not this suite. **Deny** sets are deliberately not a subject — there the same wildcard refuses more rather than approving more, so `Bash(git -C * rm *)` is correct and narrowing it would open a hole; auditing both arrays under one rule would push a maintainer to "fix" the deny entries into exactly that. Two of the four hosts are checked, because only two can express the shape: `claude.md`'s `Bash(...)` bullets, anchored to the bullet form so the section's own prose citing the unsafe pattern as a counter-example is not reported as committing it, and `opencode.md`'s allow-valued keys. `auggie.md` (anchored regexes, where `*` is a quantifier) and `antigravity.md` (token-prefix `command(git add)`, no glob grammar) are skipped and named on stderr. An empty extraction on a subject that exists is a finding, not a pass).

30. Run `scripts/audit/command-flag-hint-parity.sh` (Family 30 — every flag a command's Flags table documents also appears in that command's `argument-hint:` frontmatter. `argument-hint` is the surface a host renders when it offers the command, so a flag absent from it is one the operator is never shown: an adopter reported `--since` as "doesn't show as an option" while `review.md`'s table listed eight flags and its hint named three, and the flag was accepted by `compute-review-scope` the whole time. The logic is the `check-command-flags` primitive and this script is the shell entry point the `/audit` contract requires, so the frontmatter and fenced-block scanning reuses the runtime's tested scanners rather than a second awk copy of them — the shape §runtime-boundary principle 3 names. The subject is `framework/commands/*.md`, the sources: a generated copy carries whatever its source carries, and an adopter told their installed command disagrees with itself cannot repair it, since the fix is a ductus release. Only a `Flags` section's table rows count, and only each row's first cell, so behavior prose cross-referencing another flag is not a finding; a command documenting flags in prose rather than a table — `implement.md`'s `--auto` — is examined and contributes nothing, which is why a clean exit means every *tabled* flag is surfaced and not every documented one. Both counts go to stderr so the two claims do not read alike. An unreachable runtime is a finding rather than a silent pass, and finding no Flags table anywhere is reported as an extraction failure, not a clean corpus).

31. Run `scripts/audit/review-block-agreement.sh` (Family 31 — a spec's frontmatter `review:` block agrees with its own `review.md`. The same review is recorded twice — `last-run`/`reviewed-at`, `reviewed-against`, and the three counts — and nothing held the two together: 031 and 041 both carried `should-violations: 1` in `spec.md` while their reports recorded `0`, for weeks, because every gate reads exactly one of the two files. `check-review-gate` and `/ductus:analyze`'s review-drift check read the `spec.md` block; Family 19 resolves `reviewed-against` but never compares the counts on either side of it. A stale non-zero count reads as outstanding work that does not exist; a stale zero would hide real findings from every gate that trusts it. Both records are derived from the frontmatter on disk, and the fix names `review.md` as the source of record — it is what `/ductus:review` writes from the pass it just ran, while the block is the summary copied forward for the gates. Two consistency rules internal to the block are asserted too, because the observed failure reached the counts through them: `blocking: false` alongside a non-zero `must-violations`, and a finding waived in `review.md` with no `review.waivers` entry — the 031 root cause exactly, a waiver with no structural existence, so the count it should have retired never moved. Single-sided fields are not compared; demanding they match would invent a binding the artifacts never claimed. Subject is the intersection — specs carrying both records, the only ones that can disagree — with a `review:` block and no report left to Family 19 and `check-review-gate`. Unparseable frontmatter on a report that exists is a finding rather than a skip, and an empty subject set is a finding, since comparing nothing reports agreement).

32. Run `scripts/audit/permission-entry-shape.sh` (Family 32 — a canonical permission entry that scopes a file path with `Write(path)` rather than `Edit(path)`. Claude Code matches file permissions against `Edit` rules only, and an `Edit` rule covers every file-editing tool including `Write`, so `Write(.ductus/session.toml)` grants nothing the `Edit(...)` entry beside it already granted — and the host warns about it at every session start. Two such entries shipped in `configure/claude.md` §5 next to the very `Edit(...)` entries that made them redundant, and reached every adopter that ran `/ductus:configure`; as with Family 29, the host's own startup linter surfaced them rather than this suite. Not folded into Family 29 despite the shared subject: that family's entry grants too much, so **deny** is excluded by construction, while this one grants nothing — which is just as wrong on the deny side and arguably worse, since a `Write(path)` deny entry reads as a protected path and protects nothing. **Both** arrays are subjects here, so merging the two contracts would mean relaxing the exclusion that keeps Family 29 safe. One host is checked, because only `claude.md` has a path-scoped file-permission form: `opencode.md` scopes by tool (`"edit": "allow"`), `auggie.md` carries no file entries, and `antigravity.md` omits them deliberately under its in-workspace auto-allow; all three are named on stderr. The bare `Edit` / `Write` tool entries are not subjects — they name tools rather than paths, `Write` there is a real grant, and the extraction requires a parenthesised argument so reporting one would never push a maintainer to revoke it. Anchored to the bullet form so §5's prose naming both removed entries as a counter-example is not reported as committing them. An empty extraction on a subject that exists is a finding, not a pass).
33. Run `scripts/audit/readme-command-parity.sh` (Family 33 — every command shipped to adopters appears in the README's command tables. `/ductus:fold` shipped with spec 051 and never reached the README; neither did the rest of that feature's adopter-facing surface, and the README was the one adopter-facing artifact with no parity check behind it. Neither neighbour covers it: Family 16 pins the *installer manifest* to the command sources, and Family 30 is command-internal, checking a Flags table against that same command's own `argument-hint:`. **Presence is the assertion, not accuracy** — this family checks that a command is documented, never that its description is current or that its row enumerates its flags, because asserting either needs semantic judgment and belongs to a reviewer; folding flag-level parity in silently would make one family's name describe two checks, and the bound goes to stderr rather than being left to be inferred. `/ductus:audit`'s own absence from the README is correct and stays correct: the maintainer-only set is read from `scripts/maintainer-only-commands.txt` through `lib.sh`, the one list Family 16 and `gen-help-tables.sh` also read, since a set restated once per consumer is the drift these families exist to catch. The mirror defect is reported too — a maintainer-only command the README *does* document tells adopters about a command `/ductus` never installs. Matching is on a whole `/name` token anywhere in the file rather than on a table row, so a command documented in a callout beside its row counts and the family checks coverage rather than dictating prose. A missing README, an empty command directory, and an empty exclusion list are each findings, never a clean pass).

34. Run `scripts/audit/step-reference-integrity.sh` (Family 34 — every `step N` reference in a command file resolves to a numbered step that file has. Command files number their Instructions steps and refer to them in prose, and the numbers are the only binding; removing a step renumbers everything after it while the prose keeps the old numbers. Spec 054 did exactly that and left four stale references, one of which had silently become a **self-reference** — and that self-reference is the *only* one of the four this family catches, because the other three shifted onto steps that still exist. Deciding that "step 6" should now read "step 5" needs the semantics of what each step does; asserting it would mean matching prose against step content, and a family that fires falsely on a correct reference is worse than the silence it replaces — the standard 045 applied in rejecting the criterion-supersession check. So the family asserts only what is exact and states the gap rather than implying coverage it lacks. Three findings, because the repairs differ: `unresolved`, `self-reference` (it resolves, so an existence check passes it), and `discontinuous` (ascends from 1 but skips numbers — a partial removal's residue, which makes every later reference ambiguous rather than wrong). The check is the `check-step-references` primitive and this script is the entry point, the Family 30 shape. Subject is the **Instructions** section of `framework/commands/*.md` plus the two bootstrap procedures: a `## Markdown-only reference` sub-procedure restarts at 1 and is a different list, so its mentions are counted rather than resolved, and a file holding several numbered lists rather than one procedure — `amend.md` restarts under each subsection, `status.md` uses three one-item lists — is named in `not-a-procedure` rather than examined, since resolving against a merged set would invent findings. A qualified reference (`groom`'s step 3) names another file's procedure and is excluded by construction. Both counts go to stderr. An unreachable runtime, and an empty examined set, are findings rather than silent passes).

35. Run `scripts/audit/manifest-destination-links.sh` (Family 35 — a relative markdown link in a file the installer ships, resolved against the path that file occupies in the **adopter's** tree rather than the path it occupies here. Every other link check resolves against this repository, where a shipped file's relative links are correct by construction — which is why they were written that way — so `framework/rules/quality-cross.md`'s `../../specs/017-…`, correct at `framework/rules/`, becomes `<adopter>/specs/017-…` once the manifest copies the file to `specs/rules/` and dangles there while every check in this suite passes. It was reported from an adopter run, not caught here, and the release that shipped the dangling link also shipped the corpus-wide `check-corpus-links` pre-commit step — so the rule file, installing inside the spec root that step scans, gave the adopter a broken link and a hook that refused every commit, not merely spec commits. The subject is the manifest, split in two deliberately. **35a** takes the **Shared Files** entries, whose destinations are literal, copies each source to its destination in a throwaway tree, and runs `check-corpus-links --scope repository` there: the check is the primitive and this is the entry point, the Family 30 shape, and here it is load-bearing rather than stylistic, since a second resolver written for the adopter tree would diverge exactly as Family 26's own copy diverged from the primitive within a day of it shipping. **35b** takes the **Slash commands** entries lexically, because they cannot go through 35a for two independent reasons: their destinations carry `{config_dir}` and `ductus` placeholders with no literal form, and the primitive resolves the host config dir out of its walk by construction so it never reports an adopter's generated copies — building a tree it is designed to ignore would return a confident zero. The lexical rule is exact rather than heuristic because of what that directory contains: every ductus-authored file in it is a sibling `.md` from the same manifest, so a target holding a path separator leaves the set by definition, while a bare sibling resolves and is not a finding. What it does **not** assert is stated rather than implied — 35a proves the shipped set resolves against the ductus-authored portion of an adopter's tree, never that every link an adopter has resolves, which is a claim nothing in this repository can make — and both counts go to stderr so the two cannot read alike. An empty extraction on either table, an unreachable runtime, and an unparseable result are each findings).

36. Run `scripts/audit/self-url-resolution.sh` (Family 36 — an absolute GitHub URL pointing back into this repository that names a path which no longer exists. The form exists because of Family 35's finding: a shipped file cannot cite ductus's own spec corpus relatively, since `specs/` is this project's development record and is not in the manifest, so the repair was to name the repository instead of assuming it. That trade bought correctness in the adopter tree and spent verifiability here — a relative link is checked by Family 26 and by `check-corpus-links` on every commit, an absolute one by nobody — so a rename leaves it pointing at a 404 that only a reader clicking it discovers, and the readers of a shipped rule file are adopters, who cannot tell a ductus defect from their own. The check is the obvious one and that is the point: every such URL is `<repo>/blob/<ref>/<path>`, so strip the prefix and test the remainder against the worktree — no network, no rate limit, and stronger than the relative check it replaced because there is no depth arithmetic to get wrong. The repository slug is **derived** from the installer's own archive URL rather than hardcoded, since a literal would be a second copy of the fact under test and would make every finding wrong in a fork rather than merely absent. `main` URLs are resolved, because this project is live-on-main and a `main` URL is a claim about the current tree; a URL pinned to a tag or sha is deliberately historical, so those are counted and reported rather than resolved or silently dropped. A `{placeholder}` URL is a documented shape and is excluded by construction, as are generated command copies, whose repair is a release rather than an edit. A `blob` URL naming a directory and a `tree` URL naming a file are reported separately from an unresolved path: GitHub redirects both so neither is broken today, but the three repairs differ and one message for all three sends a maintainer looking for the wrong thing. An empty markdown corpus is a finding; zero URLs is not, and the count on stderr is what keeps a silently-broken extraction from reading as a clean corpus).

## Boundary with `/ductus:analyze`

| Concern | Owner |
| --- | --- |
| Spec's frontmatter parses; required fields present | `/ductus:analyze` |
| Dependency graph well-formed for one feature | `/ductus:analyze` |
| Rule IDs cited in spec exist in loaded rule files | `/ductus:analyze` |
| Plan / tasks / data-model present per status tier | `/ductus:analyze` |
| Cross-doc claim consistency (pipeline diagrams, back-edge wording, etc.) | `/audit` |
| Manifest / permission / registry parity | `/audit` |
| Sibling-spec coupling (bundling candidates) | `/audit` |
| Introducing-spec body drift (current-tense prose around renamed names) | `/audit` |

Rule of thumb: `/ductus:analyze` reads within one spec's directory plus its declared dependencies; `/audit` reads across the framework's cross-cutting artifacts. The two never duplicate a check.

## Output

`/audit` writes findings to stdout in a maintainer-friendly format: family header, then one finding per row with location / message / suggested-fix columns. Exit code `0` when no findings; `1` when any finding is present. No `audit.md` artifact is produced — the audit runs interactively, not stored as a per-run report.
