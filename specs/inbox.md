# Inbox

- Architectural exploration: re-frame the runtime's LLM extension points (`writeCode`, `writeSpecBody`, `assessSpecQuality`, future multi-turn points) as named Anthropic-style Skills the host loads at the seam, rather than ad-hoc JSON envelopes. Potential benefits: structural cache anchoring (Skills are a natural cache boundary); third-party hosts integrate against an emerging Skills protocol instead of govern-specific JSON; `constitution-excerpts` becomes a bundled resource rather than an inline string array. Speculative — depends on Anthropic's Skills protocol stabilizing and is a larger redesign than 022's current scope. Revisit after the writeCode payload-bundling scenario on 022 ships and the cache-anchored shape proves out the pattern. Surfaced 2026-05-19 during runtime-improvement investigation. **On hold per user 2026-07-11.**
- [ ] `/implement` hard-errors on an uncommitted spec directory instead of returning a domain outcome: `derive-boundary` computes the write boundary by diffing against the spec dir's *first commit*, so on a spec whose directory is untracked it fails with `no commits found that touch specs/{feature}` — an operational error that halts the walk at step 2, before any task runs. Nothing upstream catches it: `/specify`, `/clarify`, and `/plan` all complete and advance status on an uncommitted spec dir, so the pipeline reaches `planned` and only then discovers it cannot start. Observed 2026-07-30 on `046-scenario-open-question-visibility`, freshly created and planned in one session with no commits. Two candidate fixes, not exclusive: (a) make `derive-boundary` return an empty-boundary domain outcome with guidance ("commit the spec directory, or seed a write-boundary in the session") rather than erroring, matching how the rest of the runtime treats provable-vs-unknowable states; (b) have `/plan`'s validation gate check that the spec directory is committed before advancing `clarified → planned`, so the gap surfaces at the gate that creates it rather than one command later. Per §design-principles "never depend on human diligence", (b) is the closer fit; (a) makes the failure legible either way. Note the fail-closed rule is correct and not the defect — the defect is that the precondition is discovered late and reported as a crash rather than as a next-step.
- [ ] Sweep `026-framework-self-audit`'s three scenario open questions into `## Resolved Questions` with their trigger conditions, per the convention spec 046 settled on 2026-07-30 (an open question is an *undecided blocker*; one deferred pending a condition is resolved-with-a-condition). `audit-ci-hard-gate` ×1 and `family-10-migration-coverage` ×2 — each already states its own answer and its trigger, so this is refiling rather than deciding. 026 is `done`, so `check-artifacts` currently reports a **blocking** `scenario-open-questions` finding against it; the sweep clears that. Note the sweep is a meaningful body edit under a `done` spec, so it takes the `done → in-progress` back-edge and 026 has to re-earn `done` through the review gate — worth doing as its own focused pass rather than inside another feature's implement walk. Deferred to after 046 ships (user decision 2026-07-30, option B: let the tool flag it, handle 026 separately).
- [ ] This repo's `.govern/config.toml` has no `[host]` block, so `Host::load` falls back to the repo directory basename — `govern` — while the installed slash commands live under `.claude/commands/gov/` and are invoked as `/gov:*`. Every runtime-rendered next-action string therefore names a namespace that does not exist: `/gov:dashboard` output reads "Run /govern:target …", "/govern:clarify", "/govern:implement". Long-standing and repo-local (not a runtime defect — the fallback is documented behavior at `runtime/src/host.rs:66-75`, and `project` is explicitly the shared, committed value naming the slash-command namespace). Surfaced 2026-07-30 while implementing spec 046 task 5, whose new dashboard callout is one more place the wrong namespace appears. Fix is one block in `.govern/config.toml`: `[host]` / `project = "gov"`. Worth checking whether the framework's own dogfooding should assert this — a repo whose rendered commands do not match its installed commands is exactly the drift §drift-prevention exists to catch, and it went unnoticed because no test compares `[host] project` against the `.claude/commands/<ns>/` directory name.
- [ ] bug: `read-tasks` classifies a `- [ ] Done when: …` checkbox line as the task's done-when clause rather than a checkbox, so the /magpie:implement tally reports "all checked" while unchecked boxes remain visible. magpie's own tasks.md files were normalized to the canonical `- **Done when**: …` form (001 + 002, 27 lines), so this repo no longer trips it — what remains is reporting the parser bug against the govern runtime repo
- [ ] Spec 045's link-adjacent open-state check will fire on every completed spec's **Motivation** section unless it accounts for them, and 045's spec does not currently say how. A Motivation legitimately describes the world *before* the feature — a world that no longer exists the moment the spec ships — so its prose naturally carries 045's proposed tells (`does not exist`, `not yet`, `has no`, `stays silent`). Concrete evidence, found 2026-07-31 by running `/gov:analyze` on 046 itself: three of 046's four Motivation bullets became false the moment 046 shipped ("`/status` … carry a scenario count and nothing about their questions"; "`check-artifacts` has no family for them, so `/analyze` stays silent"; "`/target` … a feature-level target never sees them"). This is systemic rather than a 046 slip — `041-task-pruning` is `done` and its Motivation still reads "`govern` has no command to reclaim that space", false since `/prune` shipped; it only *reads* correctly because the next sentence introduces `/prune`, giving it narrative framing that a bulleted list lacks. Fixed in 046 by rewriting the section in past tense with an explicit note (commit follows this entry), which suggests one candidate resolution: **an authoring convention** — Motivation is written in past tense, so present-tense tells there are genuinely suspect. The alternatives are exempting `## Motivation` by heading (simple, but blind to a real stale claim parked there) or requiring the tell to be adjacent to a link whose target contradicts it (already 045's design — the question is whether Motivation prose links often enough to trip it). Decide during 045's clarify; the section-exemption choice changes 045's acceptance criteria.
- [ ] **Release sequence for the 045/046 work — do not tag `gvrn-v` until every box below is checked** (user decision 2026-07-30: "not going to release until 046, 045, and the inbox is groomed"). State as of 2026-07-31: (1) **046-scenario-open-question-visibility — DONE.** Scenario open questions now gate `done`, surface in `/target` and `/status`, and report as a fifth `check-artifacts` family; the constitution's `done` definition changed with it. Review clean (0 MUST / 0 SHOULD), analyze clean, 882 tests green. (2) **022-deterministic-runtime — DONE**, returned from the `in-progress` back-edge that 046's data-model edit triggered; its two new scenarios (`scenario-question-parser-fix`, `scenario-open-question-signal`) are implemented and its tasks are 5/5. Caveat worth knowing: 022's `review:` block still reads `reviewed-against: c617ba8` (2026-07-12), predating the runtime code added on 2026-07-30/31 — that code *was* reviewed, but the record lives on 046's `review.md`, not 022's. Re-run `/gov:review` against 022 if you want the record co-located. (3) **045-decision-state-drift-detection — `draft`, needs `/gov:clarify`.** It depends on 046 (now satisfied: a scenario's open-question count is readable). Read the inbox item about its Motivation-section false-positive risk **before** clarifying — it changes 045's acceptance criteria. (4) **Inbox — 6 items, needs `/gov:groom`.** (5) **Then** the release: `runtime/` changed substantially, so per AGENTS.md §Workflow a `gvrn-v<version>` tag is part of the completion gate, not a separate chore — bump `runtime/Cargo.toml`, add a `runtime/CHANGELOG.md` section, run `scripts/audit/run-all.sh` locally (it is a hard release gate), commit, tag, push the tag. Nothing is pushed yet: ~13 commits sit on local `main`.
