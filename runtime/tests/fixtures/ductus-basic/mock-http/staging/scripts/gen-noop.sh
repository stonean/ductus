#!/usr/bin/env bash
# Sample executable shipped under scripts/ — the mode-bits preservation
# fix from the post-/ductus:review patch makes sure this lands at 0o755 in
# the adopter's project directory.
exit 0
